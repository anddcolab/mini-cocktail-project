const ROOT_COCKTAILS = document.getElementById('cocktails');
const ROOT_HEADER = document.getElementById('header');
const ROOT_POP_UP = document.getElementById('pop-up-window');
const ROOT_SPINER = document.getElementById('spiner');
const ROOT_ERROR = document.getElementById('error');
const ROOT_BODY = document.body;